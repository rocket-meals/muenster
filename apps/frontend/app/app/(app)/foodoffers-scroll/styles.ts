import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  dayContainer: {
    padding: 10,
  },
  dateHeader: {
    fontSize: 18,
    marginBottom: 8,
  },
  loader: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
